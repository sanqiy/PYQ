# 项目合并分析报告

## 源文件分析

共解压 5 个压缩包，分为 4 个 PHP 原生版 + 1 个 ThinkPHP 版：

### PHP 原生版（4 个，本质同源）

| 压缩包 | 版本 | 特点 |
|--------|------|------|
| LAN3.5全解开源版去授权.zip | LAN 3.5 | 基础版，去授权（无 allkey 校验），动态版权，简单后台 |
| Miaoo朋友圈程序全开源版源码.zip | LAN 3.5 + 改版 | 比 LAN 多了 config.php、user/bobg、allkey 校验，品牌为牛角源码 |
| Miaoo朋友圈程序全开源PHP源码.zip | LAN 3.5 + 改版 | 同上，品牌为天天源码，额外带免责声明和推广页 |
| yunxi0723-3.6.zip | Yunxi Moments 3.6 | 增强版：丰富仪表盘(77KB vs 24KB)、allkey 校验、全新登录页/404页/邮件模板、优化文案 |

**差异结论**：4 个包实为同一套「LAN 朋友圈程序」的不同分发版本，代码核心逻辑一致，差异仅在品牌标识、授权校验、后台面板和文案。

### ThinkPHP 版（独立）

| 压缩包 | 说明 |
|--------|------|
| my-meimiao.zip | 基于 ThinkPHP 8 的完整重写版，功能更多（Markdown、草稿、RSS、2FA、云存储、通知等），但技术栈完全不同，无法与 PHP 版直接合并 |

## 合并方案

### 1. PHP 合并版 —— `/workspace/moments/`

**基础**：Yunxi Moments 3.6（功能最全、仪表盘最丰富、文案最优化）

**合并的改进**：
- 清除所有硬编码外部图片链接（s1.locimg.com 共 23 处），替换为动态数据库 logo
- 修复 login.php 缺少 PHP 包含和认证逻辑的 bug
- 清除品牌标识（Yunxi Moments、Miaoo、牛角源码、天天源码、云奚小屋）
- 清除 `//decode by nige112` 等注释
- 安装程序默认值改为中性品牌
- 添加 user/bobg/ 目录（背景图支持）
- 后台版权改为动态站点名
- 移除登录页的开发者工具检测脚本
- 管理员密码默认：`123456`

### 2. ThinkPHP 版 —— `/workspace/moments-thinkphp/`

直接保留 my-meimiao 的完整 ThinkPHP 8 项目，已清理 runtime 缓存。

## 使用说明

### PHP 版（moments/）
- 环境要求：PHP 7.0+，MySQL，Web Server（Apache/Nginx）
- 安装：访问 `http://你的域名/install/` 按向导安装
- 管理员默认密码：`123456`

### ThinkPHP 版（moments-thinkphp/）
- 环境要求：PHP 8.0+，MySQL，Composer，Web Server
- 需配置 `.env` 数据库连接
- 运行 `composer install` 安装依赖（vendor 已包含）
- 修改 `.example.env` 为 `.env` 并配置数据库