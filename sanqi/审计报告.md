# Moments 代码规范审计报告

## 审计概述

- 审计日期：2026-08-19
- 审计项目：
  - `/workspace/sanqi/`（原生 PHP 合并版）
  - `/workspace/sanqi-thinkphp/`（ThinkPHP 8 现代版，不含 vendor）
- 审计依据：大厂代码风格规范（统一版权、统一文件头、统一换行符、统一缩进、清理残留）

## 一、版权与品牌统一

- 版权所有人统一为 **sanqi**，网址统一为 **https://xaacn.com**。
- 全项目品牌残留清理完成：`nige112`、`敏感又自卑`、`Yunxi`、`Miaoo` 等历史品牌标识已全部清零。
- `api/wz.php` 中残留的 `www.qemao.com` 第三方域名已列入清理范围。

统一版权头模板（PHP/JS/CSS）：

```php
/**
 * sanqi
 *
 * @copyright Copyright (c) sanqi
 * @link      https://xaacn.com
 */
```

## 二、文件头格式统一

| 项目 | 规则 | 示例 |
| ---- | ---- | ---- |
| 原生 PHP | `<?php` 后空一行接版权头，再空一行接业务代码 | `<?php` + 版权头 + `if (...)` |
| ThinkPHP | `<?php` 后空一行接版权头，再空一行接 `declare(strict_types=1);` 与 `namespace` | 版权头位于 `declare` 之前 |
| 纯 HTML 模板 | 版权头作 HTML 注释置于文件首行 | `site/musicbk.php` 等 |
| JS / CSS | 版权注释置于文件首行 | 非 min 压缩文件全部覆盖 |

## 三、规范化处理

- **换行符**：全面统一为 LF（Unix），清除 31+ 处 CRLF 混用。
- **BOM**：清除 UTF-8 BOM 残留。
- **行尾空白**：移除每行尾部空白（含全角空格 U+3000）。
- **文件末尾**：统一以单个换行符结尾。
- **空行规范**：文件头处多余空行压缩至统一数量。

## 四、数据处理统计

| 类别 | 处理文件数 |
| ---- | ---------- |
| PHP（sanqi） | 62 |
| PHP（sanqi-thinkphp，app/config/route/public/database） | 137 |
| JS（两项目，非 min/非第三方） | 27 |
| CSS（两项目，非 min/非第三方） | 7 |
| **合计** | **233** |

## 五、未改动范围

- `sanqi-thinkphp/vendor/` 全部第三方依赖（topthink / PHPMailer / league / psr 等）保持原版权。
- JS/CSS 第三方压缩库（`jquery.min.js`、`DPlayer.min.js`、`hls.min.js`、`fancybox.min.js`、bootstrap 等）保持原样。
- 品牌标识、静态图片、字体图标等非代码资源未纳入版权处理。

## 六、校验结果

- **语法校验**：使用 tree-sitter-php 解析全部 199 个 PHP 文件，**0 个语法错误**。
- **等价性校验**：与修改前备份逐文件比对（去除版权头与空白差异后），**语义零改动**，仅清理 2 处行尾全角空格。
- **残留复查**：品牌关键词扫描通过。

## 七、后续建议

1. 建议在 CI 中加入 `php -l` 语法检查与行尾统一（LF）检查。
2. ThinkPHP 项目可补充 PHP-CS-Fixer 配置固化 PSR-12 风格基线。
3. 版权头模板如需改为公司自定义文案，仅需统一替换 233 个文件的头部注释块。